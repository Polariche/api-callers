apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-quick
    queries.api-callers.io/keyspace: riot
  name: api-quick
  namespace: api-callers
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 80
  selector:
    app: api-quick
    queries.api-callers.io/keyspace: riot
  type: NodePort
